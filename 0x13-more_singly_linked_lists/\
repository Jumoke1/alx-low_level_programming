#include "lists.h"
/**
 **add_nodeint_end - add a function to the end of the list
 *@head:
 *@n: int
 */
listint_t *add_nodeint_end(listint_t **head, const int n);
{
listint_t *a;

listint_t *t = *head;

a = malloc(sizeof(listint_t));
if (i == NULL)
return (NULL)

a->n = n;
a->next = NULL;

if (*head == NULL)
{
*head = a
return (a);
}
while (t->next)
t = temp->next

t->next = a;

return (a);
}

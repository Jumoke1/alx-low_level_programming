#include "main.h"
#include <stdio.h>
/**
 * main - print the number of argument passed
 * @argc: argument count
 * @argv: argument vector
 * Return: (0);
 */
int main(int argc, char *argv[])
{
printf("argc = %d\n", argc);

printf("The first line argument %s\n", argv[0]);
return (0);
}

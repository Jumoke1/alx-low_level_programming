#include "main.h"
/**
 * _isupper - isupper
 * @c: is upper case
 * Return: 1 if is c is uppercase otherwise return 0
 */
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
